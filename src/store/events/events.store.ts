import { create } from 'zustand';

interface EventState {
  // Eliminadas todas las referencias y lógica de eventos, ya que no se usan en el proyecto
}

export const useEventStore = create<EventState>((set, get) => ({
  // Eliminadas todas las referencias y lógica de eventos, ya que no se usan en el proyecto
}));
